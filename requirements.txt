# Direct third-party dependencies for the DoseRAD training stack.
#
# NOT listed here:
#   * torch     — install the wheel matching your CUDA / driver.
#   * pydosert  — VENDORED in this repo (pydosert/), not a pip dependency.
#                 Its own runtime deps are listed in submission/requirements.txt.
numpy==2.3.0
scipy==1.15.2
pandas==2.3.3
SimpleITK==2.4.1
natsort==7.1.1
matplotlib==3.10.8
tqdm==4.67.1
comet_ml==3.53.2
pymedphys==0.41.0
